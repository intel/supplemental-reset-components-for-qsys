howto_qsys_supplemental_reset_components.tex
	This is a LaTeX source file that is used to generate the PDF
	documentation for this project.

images/
	This directory contains the screen shot PNG image files that are used
	in the PDF documentation for this project.

build_documentation.sh
	This is a build script that will create the PDF documentation for this
	project.

qsys_templates/
	This directory was removed because it contains IP references that
	require the Standard tools environment and this branch of the repo is
	only supported by the Pro tools environment.
